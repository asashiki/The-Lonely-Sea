# 游戏与 Blog 接入验收记录（2026-08-24）

## 当前可验收版本

- Blog：`http://127.0.0.1:4322/`
- Studio：`http://127.0.0.1:4173/`
- Studio 正式运行时试玩：`http://127.0.0.1:4173/playtest`
- 当前游戏 release：`0.3.0-f5e62c4c`

## 已完成

- 序章改为自研 Gal Story Runtime，不再依赖 WebGAL；Studio 试玩和 Blog 正式包使用同一份运行时。
- 全画面点击推进、鼠标滚轮历史、ESC 隐藏对白、键盘选择、AUTO / SKIP 持续状态均已接通。
- 同角色差分使用硬切；登退场才播放过渡。7 套立绘按真实动作重新登记，“纸条朗读”与“害羞”不再混用；长对话会在明确情绪或动作转折处换姿势。
- 自然眨眼不绑定点击，全部 139 句语音均可驱动口型；浏览器拦截自动播放时，第一次点击只恢复当前语音，不会误跳下一句。
- SAVE / LOAD 使用 Blog 的 SAVE DATA 页面；存档截图取实际游戏画面，同 release 读档不重新加载 iframe，并按剧情块 ID 精确恢复。
- OPTION 使用 Blog 设置页；语言、音量、文字、界面、动效和指针会即时同步到游戏。清除本浏览器数据可用于重新测试首启流程。
- 首次进入先选择中 / 英 / 日语言；无存档时 Continue 禁用，游戏不再制造幽灵自动存档。
- RSS、留言、友链由 Blog 承载为 Galgame 通讯场景，游戏负责台词入口和成功 / 取消后的剧情分支，后续重新导出不会重做页面。
- 留言与友链不再暴露 GitHub 提交页面；未配置时保存在本机，部署时可通过 `PUBLIC_LIGHTHOUSE_MESSAGE_ENDPOINT` 接入统一投递端点。RSS 支持复制地址、打开订阅和查看近期文章。
- 窄屏统一强制横向；页面从横向窗口动态缩成竖向时也会立刻重新计算 16:9 舞台，不再只在首次打开时生效。
- 选项出现时保留上一句对白作为提问上下文；2～5 项使用紧凑单列，多项使用双列，选中反馈立即生效且不遮对白框。
- 正式包内置字体、游戏独立 BGM、指针、背景、七套可用 Alice 分层立绘、139 句日语 TTS 与对应口型时间线。

## 已实际验证

- 最新 release 能从 Blog START 启动，选项可点击并立即进入分支；开场四句实际切换为迎接 → 侧身闲谈 → 侧身说明 → 迎接。
- SAVE 后推进两句，再从 LOAD 读取，可回到保存时的准确台词和画面。
- 日语设置即时改变游戏台词，再切回中文后布局保持稳定。
- 390×844、700×900 与桌面尺寸均能完整显示游戏和 OPTION；横屏运行中再缩成竖屏也会同步进入强制横屏。
- 浏览器实测最终包 `0.3.0-f5e62c4c`：1024×900 与 700×900 拖动比例下逻辑舞台始终为 16:9，人物、对话框比例不漂移；LOG 不越界且可逐句重播语音。
- 实测从第二句保存、推进到第三句后读档，准确回到第二句；选项分组显示为“系统 / 视觉小说 / 更多”与“LOAD / EXTRA / OPTION”两列。
- 留言通讯层实测为完整 16:9 游戏舞台，页面无 GitHub 字样；未配置投递端点时明确仅保存在当前浏览器。
- Studio `/playtest` 与 Blog 正式包画面一致，独立试玩 SAVE 不再等待 Blog 握手。
- Studio `npx tsc --noEmit` 通过；核心测试 86/86 通过；Blog `pnpm build` 通过（0 error）。

## 有意留到素材定稿后的工作

- 目前 welcome / guide 是可替换的正式工作素材；新增表情立绘或 CG 后，只需更新 Studio 素材登记和对应剧情块再导出，不需要重写引擎或 Blog 页面。
- 全篇 139 句 TTS 已生成；生成脚本会按模型、声音、文本和 delivery 哈希复用已有结果，单句改词只需重做对应 take。
- Studio 高级模式仍保留旧 WebGAL / Terre 迁移工具；它不是当前原生发布主线，后续可单独收纳，不影响本次游戏包。
